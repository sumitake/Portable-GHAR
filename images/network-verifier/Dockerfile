# syntax=docker/dockerfile:1

ARG SOURCE_DATE_EPOCH

FROM debian:bookworm-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818 AS context-audit
COPY . /context
RUN set -eu; \
    find /context -mindepth 1 -print | while IFS= read -r candidate; do \
      relative=${candidate#/context/}; \
      case "$relative" in \
        Dockerfile|.dockerignore|build|build/portable-ghar-network-verifier|build/ca-bundle.pem|build/ca-bundle.sha256|build/ca-bundle.lock.json|build/ca-bundle.spdx.json) ;; \
        *) exit 1 ;; \
      esac; \
    done; \
    test -x /context/build/portable-ghar-network-verifier; \
    cd /context/build; \
    sha256sum -c ca-bundle.sha256; \
    printf '%s\n' verified > /context-audit.OK

FROM debian:bookworm-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818 AS layout
RUN install -d -m 0755 /etc/ssl/certs /usr/share/portable-ghar

FROM scratch
COPY --from=context-audit --chown=0:0 --chmod=0444 /context-audit.OK /context-audit.OK
COPY --from=layout /etc/ssl/certs /etc/ssl/certs
COPY --from=layout /usr/share/portable-ghar /usr/share/portable-ghar
COPY --from=context-audit --chown=65532:65532 --chmod=0555 /context/build/portable-ghar-network-verifier /usr/local/bin/portable-ghar-network-verifier
COPY --from=context-audit --chown=0:0 --chmod=0444 /context/build/ca-bundle.pem /etc/ssl/certs/ca-bundle.crt
COPY --from=context-audit --chown=0:0 --chmod=0444 /context/build/ca-bundle.lock.json /usr/share/portable-ghar/ca-bundle.lock.json
COPY --from=context-audit --chown=0:0 --chmod=0444 /context/build/ca-bundle.spdx.json /usr/share/portable-ghar/ca-bundle.spdx.json
USER 65532:65532
ENTRYPOINT ["/usr/local/bin/portable-ghar-network-verifier"]
CMD ["namespace-empty"]
